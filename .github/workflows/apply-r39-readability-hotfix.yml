name: Retired one-time R39 readability hotfix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'R40 contains the qualified readability corrections. This retired workflow must not rewrite sealed public files.'
