# rerun after profile-label repair
name: HF32 post-repair rendered smoke
on:
  push:
    branches:
      - hf32-owner-approved-simplification-20260910
    paths:
      - .github/workflows/hf32-smoke-v2.yml
permissions:
  contents: read
jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          ref: hf32-owner-approved-simplification-20260910
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Static no-loss gate
        run: |
          node --check dist/assets/hf32-public.js
          python - <<'PY'
          from pathlib import Path
          import subprocess
          pages=list(Path('dist').rglob('index.html'))
          assert len(pages)==19355
          assert all('FR-NAV1.21.0-HF3.2-CANDIDATE' in p.read_text(encoding='utf-8') for p in pages)
          assert all(p.read_text(encoding='utf-8').count('data-hf32-direct="1"')==1 for p in pages)
          assert len(list(Path('dist/profiles').glob('*/index.html')))==19103
          changed=subprocess.check_output(['git','diff','--name-only','50bf87535d7cd9bb5aabaaf5b1b93f4602549fa2...HEAD'],text=True).splitlines()
          assert not [x for x in changed if x.startswith(('server/','dist/data/'))]
          print('STATIC_NO_LOSS_PASS')
          PY
      - name: Install Playwright
        run: |
          python -m pip install --disable-pip-version-check playwright==1.55.0
          python -m playwright install chromium
      - name: Rendered smoke at owner laptop size
        run: |
          python -m http.server 8765 --directory dist >/tmp/hf32-http.log 2>&1 &
          server=$!
          trap 'kill $server || true' EXIT
          python - <<'PY'
          from playwright.sync_api import sync_playwright
          B='http://127.0.0.1:8765'
          def load(page,path,cls):
              page.goto(B+path,wait_until='domcontentloaded',timeout=15000)
              page.wait_for_function("c=>document.body&&document.body.classList.contains(c)",arg=cls,timeout=10000)
              page.wait_for_timeout(200)
          def head(page):
              assert page.locator('header .nav > a').count()==4
              assert page.locator('header .nav > details .r41-nav-more-menu > a').count()==3
          with sync_playwright() as pw:
              b=pw.chromium.launch(headless=True)
              page=b.new_page(viewport={'width':1366,'height':768})
              load(page,'/','hf32-home'); head(page); assert page.get_by_role('heading',name='Grow your visibility in the Franklin community.').count()==1; assert 'skyline.webp' in page.locator('.hf32-history img').nth(2).get_attribute('src')
              load(page,'/today/','hf32-today'); head(page); assert page.locator('main > .r22-hero .actions').count()==0; assert page.get_by_text('How we keep Today current',exact=True).count()==1
              load(page,'/get-it-done/','hf32-tasks'); head(page); assert page.get_by_role('heading',name='Choose what you want to get done').count()==1; assert page.get_by_role('button',name='See all tasks').count()==1
              load(page,'/directory/','hf32-directory'); head(page); assert page.get_by_role('heading',name='Search or choose a category').count()==1
              load(page,'/profiles/FR-ORG-5d72d3ee4e9961c5/','hf32-profile'); head(page); assert page.get_by_role('heading',name='Contact & links').count()==1; assert page.get_by_text('Own or manage this business?',exact=True).count()==1
              load(page,'/sports/','hf32-sports'); head(page); assert page.get_by_role('heading',name='Find sports in Franklin.').count()==1; assert page.locator('.hf32-sport-select').count()==1
              load(page,'/sports/bowling/','hf32-sports'); head(page); assert 'bowling' in page.locator('h1').first.inner_text().lower(); assert page.locator('.explorer-hero').evaluate("e=>e.nextElementSibling&&e.nextElementSibling.id")=='finder'; assert page.locator('[data-explorer-grid] .explorer-card').count()>=1
              load(page,'/everyday-help/','hf32-high-load-page'); head(page); assert page.locator('[data-help-card]').count()>=20; assert page.get_by_text('Emergency help',exact=True).count()==1
              load(page,'/es/deportes/','hf32-sports'); head(page); assert page.get_by_role('heading',name='Encuentre deportes en Franklin.').count()==1
              b.close()
          print('HF32_POST_REPAIR_RENDERED_SMOKE_PASS')
          PY
