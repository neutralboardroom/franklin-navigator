import fs from 'node:fs';

const write=(path,content)=>fs.writeFileSync(path,content.endsWith('\n')?content:content+'\n');
write('lib/catalog.js', "'use strict';\n\nconst COMMUNITY = 'FRANKLIN_TN';\nconst PRICE_AUTHORITY = 'FRANKLIN_COMMUNITY_MEMBERSHIP_PRICE_AUTHORITY_V4_2026_08_31';\nconst FOUNDING = Object.freeze({\n  promotionCode: 'FOUNDING30',\n  couponId: 'FRANKLIN_FOUNDING_30_2026',\n  percentOff: 30,\n  durationMonths: 12,\n  enrollmentDeadlineLocal: '2026-12-31T23:59:59-06:00',\n  appliesTo: Object.freeze(['monthly','annual']),\n  renewal: 'REGULAR_CURRENT_PRICE_UNLESS_CANCELED'\n});\n\nconst plans = Object.freeze([\n  Object.freeze({\n    id:'monthly', label:'Monthly Community Membership',\n    lookupKey:'franklin_community_member_monthly_v4',\n    billingMode:'subscription', interval:'MONTHLY',\n    regularUsd:5, foundingUsd:3.5, couponEligible:true,\n    autoRenew:true, termMonths:1, publicChoice:'MONTHLY'\n  }),\n  Object.freeze({\n    id:'annual', label:'Annual Community Membership',\n    lookupKey:'franklin_community_member_annual_v4',\n    billingMode:'subscription', interval:'ANNUAL',\n    regularUsd:50, foundingUsd:35, couponEligible:true,\n    autoRenew:true, termMonths:12, publicChoice:'ANNUAL', recommended:true\n  }),\n  Object.freeze({\n    id:'threeYearRegular', label:'Three-Year Community Membership',\n    lookupKey:'franklin_community_member_3year_regular_v4',\n    billingMode:'payment', interval:'ONE_TIME',\n    regularUsd:135, foundingUsd:null, couponEligible:false,\n    autoRenew:false, termMonths:36, publicChoice:'THREE_YEAR', foundingOnly:false\n  }),\n  Object.freeze({\n    id:'threeYearFounding', label:'Founding Three-Year Community Membership',\n    lookupKey:'franklin_community_member_3year_founding_v4',\n    billingMode:'payment', interval:'ONE_TIME',\n    regularUsd:120, foundingUsd:120, couponEligible:false,\n    autoRenew:false, termMonths:36, publicChoice:'THREE_YEAR', foundingOnly:true,\n    nonStackable:true\n  })\n]);\nconst planMap = new Map(plans.map(plan => [plan.lookupKey, plan]));\n\nfunction getPlan(lookupKey) {\n  return planMap.get(String(lookupKey || '').trim()) || null;\n}\n\nfunction publicCatalog() {\n  return {\n    community: COMMUNITY,\n    publicIdentity: 'Franklin Navigator Community Membership',\n    localOnlyMessage: 'Built specifically for Franklin, Tennessee\u2014not a national directory with a Franklin label.',\n    priceAuthority: PRICE_AUTHORITY,\n    productCount: 1,\n    publicChoiceCount: 3,\n    providerPriceCount: plans.length,\n    selfServiceRecurringPriceCount: plans.filter(plan=>plan.billingMode==='subscription').length,\n    selfServiceOneTimePriceCount: plans.filter(plan=>plan.billingMode==='payment').length,\n    founding: FOUNDING,\n    freePresence: {priceUsd:0,factualCorrectionsFree:true},\n    choices: [\n      {id:'MONTHLY',label:'Monthly',foundingUsd:3.5,regularUsd:5,autoRenew:true,renewal:'MONTHLY_UNTIL_CANCELED'},\n      {id:'ANNUAL',label:'Annual',foundingUsd:35,regularUsd:50,autoRenew:true,recommended:true,renewal:'ANNUAL_UNTIL_CANCELED'},\n      {id:'THREE_YEAR',label:'Three years',foundingUsd:120,regularUsd:135,autoRenew:false,termMonths:36,bestValue:true,foundingNonStackable:true}\n    ],\n    providerPlans: plans,\n    enterprise: {quoteOnlyAddOn:true,baseMembership:false},\n    legacy84PublicSuppressed: true,\n    publicCheckoutOpen: false\n  };\n}\n\nmodule.exports = {COMMUNITY, PRICE_AUTHORITY, FOUNDING, plans, getPlan, publicCatalog};\n");
write('lib/lifecycle.js', "'use strict';\n\nconst TERMINAL = new Set(['TERMINATED', 'REFUNDED']);\nfunction initialState() {\n  return {status:'PENDING_PAYMENT', failureCount:0, cancelAtPeriodEnd:false, currentPeriodEnd:null, lastEventCreated:0, subscriptionId:null};\n}\nfunction eventRank(type) {\n  return ({REFUNDED:100, TERMINATED:90, CANCELED:80, PAYMENT_FAILED:60, PAYMENT_SUCCEEDED:50, RENEWED:50, CHECKOUT_PENDING:10}[type] || 1);\n}\nfunction addMonthsIso(start, months) {\n  const date = new Date(start);\n  if (Number.isNaN(date.getTime())) return null;\n  const day = date.getUTCDate();\n  date.setUTCDate(1);\n  date.setUTCMonth(date.getUTCMonth() + Number(months || 0));\n  const last = new Date(Date.UTC(date.getUTCFullYear(), date.getUTCMonth()+1, 0)).getUTCDate();\n  date.setUTCDate(Math.min(day,last));\n  return date.toISOString();\n}\nfunction expirationReminders(expiresAt) {\n  const end = new Date(expiresAt);\n  if (Number.isNaN(end.getTime())) return [];\n  return [60,30,7].map(days=>({daysBefore:days,sendAt:new Date(end.getTime()-days*86400000).toISOString()}));\n}\nfunction applyEvent(input, event) {\n  const state = {...initialState(), ...(input || {})};\n  const created = Number(event.created || 0);\n  if (created && state.lastEventCreated && created < state.lastEventCreated && eventRank(event.type) < 90) return {...state, ignored:'OUT_OF_ORDER'};\n  const incomingSub = event.subscriptionId || state.subscriptionId || null;\n  const newSubscription = Boolean(event.subscriptionId && state.subscriptionId && event.subscriptionId !== state.subscriptionId);\n  if (TERMINAL.has(state.status) && !newSubscription && !['REFUNDED','TERMINATED'].includes(event.type)) return {...state, ignored:'TERMINAL_STATE'};\n  const next = {...state, subscriptionId: incomingSub, lastEventCreated: Math.max(state.lastEventCreated || 0, created || 0)};\n  if (newSubscription) Object.assign(next, initialState(), {subscriptionId:event.subscriptionId, lastEventCreated:created || 0});\n  switch (event.type) {\n    case 'CHECKOUT_PENDING': next.status='PENDING_PAYMENT'; break;\n    case 'PAYMENT_SUCCEEDED':\n    case 'RENEWED':\n      next.status='ACTIVE'; next.failureCount=0; next.cancelAtPeriodEnd=false;\n      next.currentPeriodEnd=event.currentPeriodEnd || next.currentPeriodEnd; break;\n    case 'PAYMENT_FAILED':\n      next.failureCount=(next.failureCount || 0)+1;\n      next.status=next.failureCount === 1 ? 'GRACE' : 'SUSPENDED'; break;\n    case 'CANCEL_AT_PERIOD_END':\n      next.cancelAtPeriodEnd=true; next.status='ACTIVE_CANCELING'; next.currentPeriodEnd=event.currentPeriodEnd || next.currentPeriodEnd; break;\n    case 'CANCEL_REVERSED': next.cancelAtPeriodEnd=false; next.status='ACTIVE'; break;\n    case 'TERMINATED': next.status='TERMINATED'; next.cancelAtPeriodEnd=false; break;\n    case 'REFUNDED': next.status='REFUNDED'; next.cancelAtPeriodEnd=false; break;\n    default: next.ignored='UNSUPPORTED_EVENT';\n  }\n  return next;\n}\nfunction accessAllowed(status, expiresAt=null, now=Date.now()) {\n  if (!['ACTIVE','ACTIVE_CANCELING','GRACE'].includes(String(status))) return false;\n  if (!expiresAt) return true;\n  const expiry = new Date(expiresAt).getTime();\n  return Number.isFinite(expiry) && expiry > Number(now);\n}\nfunction mapStripeEvent(event) {\n  const type = String(event?.type || '');\n  const object = event?.data?.object || {};\n  const created = Number(event?.created || object.created || 0);\n  const subscriptionId = object.subscription || (object.object === 'subscription' ? object.id : object.parent?.subscription_details?.subscription) || null;\n  let currentPeriodEnd = object.current_period_end ? new Date(object.current_period_end * 1000).toISOString() : null;\n  const termMonths = Number(object.metadata?.term_months || object.subscription_details?.metadata?.term_months || 0);\n  const autoRenew = String(object.metadata?.auto_renew || object.subscription_details?.metadata?.auto_renew || '').toLowerCase();\n  if (!currentPeriodEnd && type === 'checkout.session.completed' && object.mode === 'payment' && termMonths === 36 && autoRenew === 'false') {\n    currentPeriodEnd = addMonthsIso(new Date(created*1000 || Date.now()), 36);\n  }\n  if (type === 'checkout.session.completed') return {type: object.payment_status === 'paid' ? 'PAYMENT_SUCCEEDED' : 'CHECKOUT_PENDING', created, subscriptionId:object.subscription || null, currentPeriodEnd, oneTimeTerm:object.mode==='payment'};\n  if (type === 'checkout.session.async_payment_succeeded') return {type:'PAYMENT_SUCCEEDED', created, subscriptionId, currentPeriodEnd};\n  if (type === 'checkout.session.async_payment_failed' || type === 'invoice.payment_failed') return {type:'PAYMENT_FAILED', created, subscriptionId, currentPeriodEnd};\n  if (type === 'invoice.paid' || type === 'invoice.payment_succeeded') return {type: object.billing_reason === 'subscription_cycle' ? 'RENEWED' : 'PAYMENT_SUCCEEDED', created, subscriptionId, currentPeriodEnd};\n  if (type === 'customer.subscription.updated') return {type: object.cancel_at_period_end ? 'CANCEL_AT_PERIOD_END' : 'CANCEL_REVERSED', created, subscriptionId:object.id, currentPeriodEnd};\n  if (type === 'customer.subscription.deleted') return {type:'TERMINATED', created, subscriptionId:object.id, currentPeriodEnd};\n  if (type === 'charge.refunded') return {type:'REFUNDED', created, subscriptionId, currentPeriodEnd};\n  return null;\n}\nmodule.exports = {initialState, applyEvent, accessAllowed, mapStripeEvent, addMonthsIso, expirationReminders};\n");
write('test/catalog.test.js', "'use strict';\nconst test=require('node:test');const assert=require('node:assert/strict');\nconst {plans,publicCatalog,getPlan,FOUNDING,PRICE_AUTHORITY}=require('../lib/catalog');\ntest('V4 catalog has one public membership, three choices and four provider prices',()=>{const catalog=publicCatalog();assert.equal(catalog.productCount,1);assert.equal(catalog.publicChoiceCount,3);assert.equal(catalog.providerPriceCount,4);assert.equal(plans.length,4);assert.equal(new Set(plans.map(x=>x.lookupKey)).size,4);assert.equal(catalog.legacy84PublicSuppressed,true);assert.match(PRICE_AUTHORITY,/V4/);});\ntest('V4 amounts and renewal behavior are exact',()=>{const by=id=>plans.find(x=>x.id===id);assert.equal(by('monthly').regularUsd,5);assert.equal(by('monthly').foundingUsd,3.5);assert.equal(by('annual').regularUsd,50);assert.equal(by('annual').foundingUsd,35);assert.equal(by('threeYearRegular').regularUsd,135);assert.equal(by('threeYearFounding').regularUsd,120);for(const plan of plans)assert.equal(getPlan(plan.lookupKey),plan);assert.equal(by('threeYearRegular').autoRenew,false);assert.equal(by('threeYearFounding').couponEligible,false);assert.equal(by('threeYearFounding').nonStackable,true);assert.deepEqual(FOUNDING.appliesTo,['monthly','annual']);});\n");
write('test/lifecycle.test.js', "'use strict';\nconst test=require('node:test');const assert=require('node:assert/strict');\nconst {initialState,applyEvent,accessAllowed,mapStripeEvent,addMonthsIso,expirationReminders}=require('../lib/lifecycle');\nconst at=(type,created=1,extra={})=>({type,created,...extra});\ntest('redirect pending never activates',()=>assert.equal(applyEvent(initialState(),at('CHECKOUT_PENDING')).status,'PENDING_PAYMENT'));\ntest('settled payment activates',()=>assert.equal(applyEvent(initialState(),at('PAYMENT_SUCCEEDED',2,{subscriptionId:'sub_1'})).status,'ACTIVE'));\ntest('failure grace then suspension and recovery',()=>{let s=applyEvent(initialState(),at('PAYMENT_SUCCEEDED',1));s=applyEvent(s,at('PAYMENT_FAILED',2));assert.equal(s.status,'GRACE');assert.equal(accessAllowed(s.status),true);s=applyEvent(s,at('PAYMENT_FAILED',3));assert.equal(s.status,'SUSPENDED');s=applyEvent(s,at('PAYMENT_SUCCEEDED',4));assert.equal(s.status,'ACTIVE');assert.equal(s.failureCount,0);});\ntest('cancel at period end preserves access',()=>{let s=applyEvent(initialState(),at('PAYMENT_SUCCEEDED',1));s=applyEvent(s,at('CANCEL_AT_PERIOD_END',2,{currentPeriodEnd:'2027-01-01T00:00:00Z'}));assert.equal(s.status,'ACTIVE_CANCELING');assert.equal(accessAllowed(s.status,'2027-01-01T00:00:00Z',Date.parse('2026-01-01')),true);s=applyEvent(s,at('CANCEL_REVERSED',3));assert.equal(s.status,'ACTIVE');});\ntest('termination and refund are terminal',()=>{let s=applyEvent(initialState(),at('PAYMENT_SUCCEEDED',1,{subscriptionId:'sub_1'}));s=applyEvent(s,at('TERMINATED',2));assert.equal(s.status,'TERMINATED');assert.equal(applyEvent(s,at('PAYMENT_SUCCEEDED',3,{subscriptionId:'sub_1'})).status,'TERMINATED');s=applyEvent(s,at('PAYMENT_SUCCEEDED',4,{subscriptionId:'sub_2'}));assert.equal(s.status,'ACTIVE');s=applyEvent(s,at('REFUNDED',5));assert.equal(s.status,'REFUNDED');});\ntest('old nonterminal events are ignored',()=>{let s=applyEvent(initialState(),at('PAYMENT_SUCCEEDED',10));assert.equal(applyEvent(s,at('PAYMENT_FAILED',9)).status,'ACTIVE');});\ntest('one-time three-year checkout creates a 36-month entitlement expiry',()=>{const created=Date.UTC(2026,7,31)/1000;const mapped=mapStripeEvent({type:'checkout.session.completed',created,data:{object:{mode:'payment',payment_status:'paid',metadata:{term_months:'36',auto_renew:'false'}}}});assert.equal(mapped.type,'PAYMENT_SUCCEEDED');assert.equal(mapped.subscriptionId,null);assert.equal(mapped.currentPeriodEnd,addMonthsIso(new Date(created*1000),36));assert.equal(expirationReminders(mapped.currentPeriodEnd).length,3);});\ntest('expired one-time entitlement is not allowed',()=>assert.equal(accessAllowed('ACTIVE','2026-01-01T00:00:00Z',Date.parse('2026-02-01')),false));\ntest('Stripe event mapping covers recurring lifecycle',()=>{assert.equal(mapStripeEvent({type:'invoice.paid',created:2,data:{object:{billing_reason:'subscription_cycle'}}}).type,'RENEWED');assert.equal(mapStripeEvent({type:'customer.subscription.updated',created:3,data:{object:{id:'sub_x',cancel_at_period_end:true}}}).type,'CANCEL_AT_PERIOD_END');assert.equal(mapStripeEvent({type:'charge.refunded',created:4,data:{object:{}}}).type,'REFUNDED');});\n");
write('test/server.test.js', "'use strict';\nconst test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');\nconst source=fs.readFileSync(require.resolve('../server'),'utf8');\ntest('runtime has production endpoints and V4 fail-closed readiness',()=>{for(const route of ['/health','/ready','/api/accounts/register','/api/membership/start','/api/billing/portal','/webhooks/stripe','/internal/sre/events','/admin/payment-links/sync'])assert.ok(source.includes(route),route);assert.ok(source.includes('links===4'));assert.ok(source.includes('links.length!==4'));assert.ok(source.includes('STRIPE_WEBHOOK_SECRET'));assert.ok(source.includes('STRIPE_PORTAL_LOGIN_URL'));});\ntest('checkout applies Founding30 only to coupon-eligible monthly and annual plans',()=>{assert.ok(source.includes('if(plan.couponEligible)'));assert.ok(source.includes('couponEligible:plan.couponEligible'));assert.equal(source.includes('autoRenew:true}}'),false);});\ntest('access checks include entitlement expiry',()=>assert.ok(source.includes('accessAllowed(membership.status,membership.current_period_end)')));\ntest('preflight validates actual origin without spreading IncomingMessage',()=>{assert.ok(source.includes(\"if(req.method==='OPTIONS')\"));assert.equal(source.includes(\"enforceOrigin({...req\"),false);});\ntest('profile IDs are strict and case-preserving',()=>{assert.ok(source.includes('PROFILE_RE'));assert.ok(source.includes('return id'));});\n");

let server=fs.readFileSync('server.js','utf8');
const replacements=[
  ["const RELEASE = process.env.LOCAL_RELEASE || 'FR-NAV1.0.0-CANDIDATE-R25';","const RELEASE = process.env.LOCAL_RELEASE || 'FR-NAV1.3.0-CANDIDATE-R28';"],
  ['links===84','links===4'],
  ["if(links.length!==84)throw publicError('PAYMENT_LINK_COUNT_INVALID','Exactly 84 payment links are required.');","if(links.length!==4)throw publicError('PAYMENT_LINK_COUNT_INVALID','Exactly four V4 checkout mappings are required.');"],
  ["JSON.stringify({vertical:plan.vertical,tier:plan.tier,interval:plan.interval})","JSON.stringify({planId:plan.id,billingMode:plan.billingMode,termMonths:plan.termMonths,autoRenew:plan.autoRenew,couponEligible:plan.couponEligible})"],
  ["const checkoutUrl=addLinkParams(link.url,{client_reference_id:intentId,locked_prefilled_email:session.email,prefilled_promo_code:FOUNDING.promotionCode});return sendJson(req,res,200,{ok:true,checkoutUrl,intentId,plan:{lookupKey:plan.lookupKey,regularUsd:plan.regularUsd,foundingUsd:plan.foundingUsd,autoRenew:true}},reqId);","const checkoutParams={client_reference_id:intentId,locked_prefilled_email:session.email};if(plan.couponEligible)checkoutParams.prefilled_promo_code=FOUNDING.promotionCode;const checkoutUrl=addLinkParams(link.url,checkoutParams);return sendJson(req,res,200,{ok:true,checkoutUrl,intentId,plan:{lookupKey:plan.lookupKey,regularUsd:plan.regularUsd,foundingUsd:plan.foundingUsd,autoRenew:plan.autoRenew,termMonths:plan.termMonths,billingMode:plan.billingMode,couponEligible:plan.couponEligible}},reqId);"],
  ["tier:getPlan(membership.lookup_key)?.tier","membershipType:'COMMUNITY_MEMBERSHIP',planId:getPlan(membership.lookup_key)?.id"],
  ["accessAllowed(membership.status)","accessAllowed(membership.status,membership.current_period_end)"]
];
for(const [from,to] of replacements){if(!server.includes(from))throw new Error('missing_server_pattern:'+from.slice(0,80));server=server.replaceAll(from,to);}
write('server.js',server);

let render=fs.readFileSync('render.yaml','utf8');
render=render.replace(/FR-NAV1\.2\.0-CANDIDATE-R27/g,'FR-NAV1.3.0-CANDIDATE-R28');
write('render.yaml',render);

let proof=fs.readFileSync('.github/workflows/franklin-commerce-runtime-persistent-proof.yml','utf8');
const proofReplacements=[
 ['LOCAL_RELEASE: FR-NAV1.2.0-CANDIDATE-R27','LOCAL_RELEASE: FR-NAV1.3.0-CANDIDATE-R28'],
 ['Synchronize exactly 84 synthetic non-charging mappings','Synchronize exactly four V4 synthetic non-charging mappings'],
 ["if(plans.length!==84) throw new Error('catalog_not_84');","if(plans.length!==4) throw new Error('catalog_not_4');"],
 ["jq -e '.ok==true and .count==84'","jq -e '.ok==true and .count==4'"],
 [".paymentLinkCount==84", ".paymentLinkCount==4"],
 ["franklin_legal_individual_monthly_v2","franklin_community_member_monthly_v4"],
 ["jq -e '.selfServiceRecurringPriceCount==84 and .productCount==14 and ([.verticals[].enterprise.quoteOnly]|all)' /tmp/catalog.json","jq -e '.providerPriceCount==4 and .publicChoiceCount==3 and .productCount==1 and .legacy84PublicSuppressed==true and .publicCheckoutOpen==false' /tmp/catalog.json"],
 ["jq -e '([.verticals[].plans[]|select(.tier==\"ENTERPRISE\")]|length)==0' /tmp/catalog.json","jq -e '.enterprise.quoteOnlyAddOn==true and .enterprise.baseMembership==false' /tmp/catalog.json"],
 ["select count(*) from franklin_payment_links where active=true\")\" = '84'","select count(*) from franklin_payment_links where active=true\")\" = '4'"],
 ["schemaVersion:'franklin.commerce.persistent-proof.v2'","schemaVersion:'franklin.commerce.persistent-proof.v3'"],
 ["'WO-20260831-LOCAL-FRANKLIN-R26-RUNTIME-DEPLOY-AND-LIVE-PAYMENT-CUTOVER-025'","'WO-20260831-LOCAL-FRANKLIN-R26-RUNTIME-DEPLOY-AND-LIVE-PAYMENT-CUTOVER-025','WO-20260831-LOCAL-FRANKLIN-R27-NO-BRAINER-MEMBERSHIP-AND-SALES-FUNNEL-032','WO-20260831-SCC-LOCAL-R27-RUNTIME-ARCHIVE-COMMERCE-COMPLETION-034'"],
 ["exact84Mapping:'PASS',enterpriseQuoteOnlyNegativeTest:'PASS'","exactV4ProviderMapping:'PASS',legacy84PublicSuppression:'PASS',threeYearNoAutoRenew:'PASS'"]
];
for(const [from,to] of proofReplacements){if(!proof.includes(from))throw new Error('missing_proof_pattern:'+from.slice(0,100));proof=proof.replaceAll(from,to);}
write('.github/workflows/franklin-commerce-runtime-persistent-proof.yml',proof);

const evidence={
 schemaVersion:'franklin.r28.runtime-source-upgrade.v1',
 release:'FR-NAV1.3.0-CANDIDATE-R28',
 priceAuthority:'FRANKLIN_COMMUNITY_MEMBERSHIP_PRICE_AUTHORITY_V4_2026_08_31',
 providerPriceCount:4,publicChoiceCount:3,legacy84PublicSuppressed:true,
 threeYearOneTime36Months:true,threeYearAutoRenew:false,
 foundingCouponAppliesOnlyTo:['monthly','annual'],
 commerceEnabledDefault:false,realChargeCreated:false,authorityTransfer:false
};
write('evidence/R28_V4_RUNTIME_SOURCE_UPGRADE_RECEIPT.json',JSON.stringify(evidence,null,2));
console.log(JSON.stringify(evidence));
